# Web / WSGI
gunicorn
flask
markupsafe

# Persistence (Postgres on CSC Pukki)
SQLAlchemy>=2.0
psycopg2-binary

# Scientific computing
numpy
scipy
networkx
pandas
pillow

# Chemistry
rdkit
multiset
rxn-insight
pubchempy

# Config & logging
omegaconf
hydra-core
pyyaml
wandb

# ML framework
pytorch-lightning

# Visualization (top-level imports in diffusion helpers)
matplotlib
seaborn
imageio

# Evaluation metrics (top-level import in setup.py)
fcd_torch

# Syntheseus retrosynthesis planning framework.
# [local-retro] adds the LocalRetro second single-step model (syntheseus-local-retro
# + dgllife; pulls pandas<2.2). dgl has no torch-2.6 build, so dgl is pinned to 1.1.3
# (predates graphbolt's hard torch-version check; fine for dgllife featurization), and
# torchdata to <0.10 (dgl 1.1.3 imports torchdata.datapipes, removed in 0.10).
syntheseus[local-retro]
dgl==1.1.3
torchdata==0.9.0

# Misc
tqdm

# CPU-only torch
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu

# PyG packages — must match torch version + cpu
--find-links https://data.pyg.org/whl/torch-2.6.0+cpu.html
torch-geometric==2.6.1
torch-scatter==2.1.2
torch-sparse==0.6.18
torch-cluster==1.6.3
torch-spline-conv==1.2.2